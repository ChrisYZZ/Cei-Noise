Cei-Noise 项目总结报告
摘要
Cei-Noise 项目是一个基于 Vue 3、Vite 和 Cesium 的 3D 城市噪声监测与可视化系统，旨在模拟无人机（eVTOL/UAV）在城市低空环境中的噪声传播、路径规划和空域管理。该系统前端实现 3D 城市模型渲染、无人机路径动画和噪声热图显示，后端使用 FastAPI 和 Python 处理噪声计算、路径优化、空域容量分析以及风险评估。本报告聚焦数据处理、噪声处理、空域衡量与路径规划的核心方面，评估其合理性和改进方向。项目数据来源于公开来源（如广州市建筑/POI 数据），噪声模型参考国际标准（如 ISO 9613-2），路径规划基于学术综述（如《城市低空航路规划研究综述》）。整体设计注重实时性和交互性，适用于城市空中交通（UAM）噪声监测。

1. 项目概述
Cei-Noise 系统模拟无人机在广州市低空环境中的飞行，结合噪声传播模型和路径规划，实现 3D 可视化监测。主要组件包括：

前端：使用 Cesium 渲染 3D 城市模型（3D Tiles）、无人机动画和噪声热图（cesium-heatmap-es6）。
后端：FastAPI 服务处理噪声计算、路径优化、空域分析和风险评估。
目标：评估城市噪声影响，支持低空经济规划（如医疗快递、物流线）。
数据来源：建筑数据（CSV 转换为 3D Tiles）、POI（广州市规划局 GeoJSON）、噪声/路径（模拟或预定义）。
开发状态：前端基本完成，后端功能实现但需优化（如集成真实 GIS 数据）。
系统强调科学依据：噪声模型参考 ISO 9613-2:2024，路径规划参考《城市低空航路规划研究综述》（张宏海 等，2021）中的 NTSC 指标和容量评估。

2. 数据处理
数据处理是系统基础，涉及读取、转换、整合和优化，确保高效加载和渲染。流程分为前端加载和后端计算两部分。

2.1 数据读取与来源
3D 城市模型数据：源自广州市建筑 CSV 文件（包含建筑物名称、多边形坐标、高度）。每个记录为 Polygon 几何（如 [(113.793599, 23.758754), ...]），总规模较大（GB 级）。
噪声数据：两部分来源：
前端示例：借鉴美国国家运输噪声地图（https://maps.dot.gov/BTS/NationalTransportationNoiseMap/），栅格化分贝数据（Lden），但无广州相关性（后续删除）。
后端生成：基于预定义飞行路径（FLIGHT_ROUTES，硬编码在 main.py），结合国际标准动态计算。
地理数据 (POI)：从广州市规划和自然资源局获取 GeoJSON 文件，包含点位（如医院、机场）。
无人机路径数据：后端预定义 5 条路线（如珠江新城 CBD 线），每个路径为经纬度、高度、时间序列的列表。
其他：无人机模型（GLB 文件）、Cesium 库（本地）。
2.2 数据转换与处理步骤
CSV 到 3D Tiles：使用 Cesium ion（原 Cesium Lab）云平台：
解析 CSV 坐标字符串为 GeoJSON Polygon（使用 GeoPandas/Shapely）。
上传 GeoJSON，配置挤出（extrusion）基于高度属性，启用 LOD（分级细节）压缩。
输出 tileset.json 和瓦片文件夹，置于 public/GZ3dtile/，加载时间优化至秒级。
噪声数据处理：后端 generateNoiseGeoJSON 函数生成网格化 GeoJSON：
确定路径边界，扩展缓冲区（0.005° ≈ 500m）。
网格采样（步长 ≈50m），对每个点插值路径位置，计算噪声（详见噪声处理）。
输出 FeatureCollection，包含噪声属性（noise, maxNoise, minNoise）。
路径数据：后端 FLIGHT_ROUTES 列表线性插值（10 步），前端转换为 Cesium SampledPositionProperty 动画。
优化：分批处理大 CSV（避免内存溢出）；前端使用 Vite 压缩；后端无持久存储，实时计算。
验证：确保坐标系一致（WGS84），高度单位米。潜在风险：大文件加载瓶颈，使用 Web Workers 缓解。
数据处理注重效率和兼容性，支持多层切换（POI/热图/噪声）。

3. 噪声处理
噪声处理核心在后端 main.py 的 calculateNoiseAtPoint 函数，模拟无人机噪声传播。模型参考 ISO 9613-2:2024，但简化实现。

3.1 噪声模型实现
核心公式：
水平距离：Haversine 公式计算地面点到无人机位置的距离。
斜距：distance = sqrt(horizontal^2 + height^2)。
衰减：20 * log10(distance) + 0.005 * distance（球面扩散 + 大气吸收）。
地面效应：若高度 <100m，-3 * (1 - height/100)（增强低空噪声）。
输出：base_noise - attenuation，最小为 0 dB。
生成过程：generateNoiseGeoJSON 函数：
输入路径和基础噪声（e.g., 82 dB）。
网格采样，插值路径点（10 步），计算每个网格点的最大/最小噪声。
转换为 GeoJSON 或热图数据（放大 10 倍适配 Cesium）。
前端可视化：API /noise 返回 points 数组，渲染为 6 级热图（40-80 dB，绿到红）。
3.2 与标准/论文对比
ISO 9613-2:2024：标准公式为 L_p = L_w - 20 log10(r) - A_atm - A_gr - A_bar - A_misc（忽略方向性 D）。代码简化 A_atm 为 0.005*distance（频率无关），忽略地面反射 (A_gr)、障碍 (A_bar)。合理性：适用于室外传播，但忽略频率/湿度导致偏差（高频吸收更强）。
Rizzi et al. (2023)：eVTOL 噪声包括宽频带（100-2000 Hz）和谐波，需考虑方向性和动态轨迹。代码未建模这些，假设常量噪声，忽略多普勒效应。
合理性评估：模型捕捉基本衰减，适用于初步模拟。但简化过度：在复杂城市（建筑反射）中误差大（可达 10-20 dB）。
不足：
无频率依赖吸收（忽略温度/湿度）。
未考虑环境因素（如风、反射）。
静态假设，无动态路径影响。
改进方向：
集成 acoustics 库，实现完整 ISO 公式（添加 A_atm 表查询）。
加入 eVTOL 噪声谱（宽频/谐波），参考 Rizzi 方向性模型。
添加验证：对比实测数据或 NASA 工具。
动态模拟：融入风场粒子效果。
4. 空域衡量与路径规划
路径规划和空域衡量在后端 airspace_capacity.py、ntsc_calculator.py 和 main.py 中实现，参考论文 NTSC 和容量模型。

4.1 路径规划
实现：/optimize_route API 使用 RouteOptimizer（矩阵方法生成路径），输入起点/终点，输出优化路径（避障列表为空时简化）。
冲突检测：ntsc_calculator.py 计算 NTSC（冲突时间/总飞行时间）：
输入飞行段（FlightSegment），时间步 1s 插值位置。
检查水平（50m）/垂直（30m）间隔违反，Haversine 距离。
输出 NTSC 值、安全等级（<0.01 为 EXCELLENT）。
地面风险：ground_risk_evaluator.py 评估撞击概率（影响面积 * 密度）、严重度（动能），缓解因子（降落伞等），输出风险水平和建议。
依据：论文综述的静态/动态规划，A* 变体检测冲突。
4.2 空域衡量
容量分析：AirspaceCapacityAnalyzer.calculate_route_capacity：
路径长度（Haversine 累加）。
最大飞机数：长度 / (50m 间隔 + 10m 飞机长)。
吞吐量：基于 50 km/h 速度，密度限 10 架/km²。
冲突概率：analyze_conflict_probability，基于交叉点（距离 <100m）和密度因子。
整体评估：/airspace_analysis API 输出概率、容量和建议（如增加间隔）。
合理性：符合论文 NTSC 和容量定义，适用于低空多无人机。但简化（固定密度、无实时流量），忽略风/动态避碰。
不足：
障碍数据缺失（需集成 3D Tiles 建筑）。
无高级算法（如 RRT）。
改进方向：
集成 NetworkX 实现 A*/RRT 规划。
添加实时流量模拟，提升 NTSC 精度。
耦合噪声：路径优化最小化噪声影响。
5. 结论与未来工作
Cei-Noise 系统有效处理数据、噪声和空域规划，支持 UAM 噪声监测。但模型简化需优化，以提升准确性。未来：集成真实 GIS、验证模型、扩展多无人机模拟。
